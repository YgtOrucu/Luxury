@model MarketDataDto

<section class="market-section" id="market-data" aria-label="Canlı Piyasa Verileri">
    <div class="container-xl">

        <div class="section-header text-center mb-5">
            <p class="section-eyebrow">Anlık Veriler</p>
            <h2 class="section-title">Piyasa Takibi</h2>
            <p class="section-sub">Seyahat planlamanızı destekleyen canlı veriler</p>
        </div>

        <div class="row g-4" role="list" aria-label="Piyasa veri kartları">
            @await Component.InvokeAsync("LuxuryMarketsectionCurrency", new { Currency = Model?.Currency })
            @await Component.InvokeAsync("LuxuryMarketsectionWeather", new { Weather = Model?.Weather })
            @await Component.InvokeAsync("LuxuryMarketsectionCoins", new { Coins = Model?.Coins })
            @await Component.InvokeAsync("LuxuryMarketsectionFuel", new { Fuel = Model?.Fuel })
        </div>
    </div>
</section>